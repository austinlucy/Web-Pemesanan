const DB_CONFIG = {
  host: 'localhost',
  port: 3306,
  user: 'root',
  password: '',
  database: 'pemesanan_gedung',
  waitForConnections: true,
  connectionLimit: 10,
  queueLimit: 0,
};

module.exports = DB_CONFIG;
